# question2.py

This Python file contains a function called `find_and_replace`.

It goes through a list and replaces all occurrences of a given value with another value.

If the input is not a list, it will print an error message.

## How to run

Use this in the terminal:

```bash
python3 question2.py
